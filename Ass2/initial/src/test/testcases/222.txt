
        Function: foo
            Body:
            If bool_of_string ("True") Then 
                a = int_of_string (read (b));
                b = float_of_int (a) +. 2.0;
            EndIf.
            EndBody.